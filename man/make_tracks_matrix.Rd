% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compartments.R
\name{make_tracks_matrix}
\alias{make_tracks_matrix}
\title{Import tracks and arrange scores into a bin-by-track matrix}
\usage{
make_tracks_matrix(
  tracks,
  track_names,
  bins_gr,
  keeping_bins = "all",
  cores = 4
)
}
\arguments{
\item{tracks}{Named vector of track file paths.}

\item{track_names}{Character vector of track/fraction names.}

\item{bins_gr}{\code{GRanges} of the genomic bins.}

\item{keeping_bins}{Integer vector of bin indices to keep, or \code{"all"}.}

\item{cores}{Number of cores for parallel steps.}
}
\value{
A list with \code{bws_dtable} (bin-by-track \code{data.table}) and
\code{keeping_bins} (0-based bin indices).
}
\description{
Imports and re-bins the tracks, selects the bins to keep, extracts the
per-bin scores and assembles them into a \code{data.table} (rows = bins,
columns = tracks). Returns 0-based bin indices for CALDER.
}
\keyword{internal}
