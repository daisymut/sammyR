% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compartments.R
\name{call_subcompartments_sammy}
\alias{call_subcompartments_sammy}
\title{Call SAMMY-seq sub-compartments for a set of samples}
\usage{
call_subcompartments_sammy(
  patients,
  tracks_db,
  bins_gr,
  subs_file,
  binsize,
  chr,
  genes_gr,
  keeping_bins1 = "all",
  sublevel = "sub.8",
  sub_colors = c(B.2.2 = "#4575b4", B.2.1 = "#74add1", B.1.2 = "#abd9e9", B.1.1 =
    "#e0f3f8", A.1.1 = "#fee090", A.1.2 = "#fdae61", A.2.1 = "#f46d43", A.2.2 =
    "#d73027"),
  cores = 4,
  n.comp = 10,
  const.comp = 5
)
}
\arguments{
\item{patients}{Character vector of sample names to analyse.}

\item{tracks_db}{Data frame with columns \code{Patient_name},
\code{Fraction} and \code{File} (one row per fraction/track).}

\item{bins_gr}{\code{GRanges} of the genomic bins.}

\item{subs_file}{Path to the \code{.Rdata} file where the returned list of
sub-compartment objects is saved.}

\item{binsize}{Integer bin size in base pairs.}

\item{chr}{Chromosome name to analyse.}

\item{genes_gr}{\code{GRanges} of gene annotations, used to orient the eigenvector.}

\item{keeping_bins1}{Integer vector of 1-based bin indices to keep, or
\code{"all"} to use every bin in \code{bins_gr}.}

\item{sublevel}{Sub-compartment resolution level (e.g. \code{"sub.8"}, \code{"sub.2"}).}

\item{sub_colors}{Named character vector mapping sub-compartment labels to colours.}

\item{cores}{Number of cores passed to \code{\link[parallel]{mclapply}}.}

\item{n.comp}{Number of components for the CALDER sub-compartment call.}

\item{const.comp}{Number of constrained components for the CALDER call.}
}
\value{
A named list (one element per sample), each containing
\code{sammy_blocks}, \code{sammy_blocks_trend}, \code{subcompartment_obj},
\code{annotrack}, \code{gr} and \code{bin_eigenvector}. The same list is
saved to \code{subs_file} as a side effect.
}
\description{
Wrapper that computes distance and correlation matrices, calls CALDER
blocks and sub-compartments, derives the bin-level eigenvector and returns,
per sample, all the objects needed for downstream export and plotting.
Intermediate matrices are cached to \code{.Rdata} files and reused if present.
}
\examples{
\dontrun{
sub_objs <- call_subcompartments_sammy(
  patients = "P1", tracks_db = comp_df, bins_gr = bins_gr,
  subs_file = "P1_sub.Rdata", binsize = 50000L, chr = "chr1",
  genes_gr = genes_gr, sublevel = "sub.2"
)
}
}
