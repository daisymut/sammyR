% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compartments.R
\name{filter_bins}
\alias{filter_bins}
\title{Filter Re-binned Tracks to a Subset of Bins}
\usage{
filter_bins(bws, track_names, keeping_bins, bins_gr, cores = 4)
}
\arguments{
\item{bws}{Named list of GRanges tracks (output of import_and_rebin__bw).}

\item{track_names}{Character vector of track names to process.}

\item{keeping_bins}{Either the string "all" or a numeric vector of bin indices.}

\item{bins_gr}{GRanges of the full bin set (used to size the "all" case).}

\item{cores}{Integer number of cores for parallel::mclapply (default 4).}
}
\value{
A list with \code{bws} (filtered track list) and \code{keeping_bins} (resolved
numeric index vector).
}
\description{
Keeps only the requested bins across a set of re-binned tracks, dropping the
rest (e.g. bins carrying NA rows in Hi-C). If \code{keeping_bins} is "all", every
bin is retained; if numeric, only those indices are kept.
}
\keyword{internal}
