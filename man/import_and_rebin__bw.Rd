% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{import_and_rebin__bw}
\alias{import_and_rebin__bw}
\title{Import BigWig Files and Re-bin to Common Intervals}
\usage{
import_and_rebin__bw(files, bin_list, names, genome = NULL, cores = 1)
}
\arguments{
\item{files}{Character vector of paths to BigWig files.}

\item{bin_list}{A GRanges of target bins to average over.}

\item{names}{Character vector of names for the returned list, one per file.}

\item{genome}{Optional genome tag (e.g. "hg38"); if NULL (default) not set.}

\item{cores}{Integer for parallel::mclapply. Default 1 (serial); >1
parallelises across files (ignored on Windows).}
}
\value{
A named list of GRanges, each with a \code{score} metadata column.
}
\description{
Imports one or more BigWig files as RleList and computes the binned-average
signal over a shared set of bins. Seqlevels are aligned to \code{bin_list} before
binning so tracks from different sources are comparable. Signal type is
irrelevant: single-fraction coverage or MLE ratios are handled identically.
}
