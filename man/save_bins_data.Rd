% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{save_bins_data}
\alias{save_bins_data}
\title{Save bins data to a structured CSV}
\usage{
save_bins_data(
  data_list,
  current_ratio,
  comparison_name,
  file_suffix,
  g1 = NULL,
  g2 = NULL
)
}
\arguments{
\item{data_list}{List of data frames to combine. If empty, nothing is written.}

\item{current_ratio}{Character. Ratio/contrast identifier, used in the filename.}

\item{comparison_name}{Character. Comparison name, used in the filename.}

\item{file_suffix}{Character. Suffix selecting the export type; if it matches
"all_bins", the \code{fraction}/\code{direction} columns are dropped.}

\item{g1, g2}{Optional character. Reference and test group names; when both are
given, their per-group statistic columns are kept in filtered exports.}
}
\value{
Invisibly \code{NULL}; called for its side effect of writing a CSV.
}
\description{
Row-binds a list of per-category bin data frames, keeps the relevant columns
depending on whether this is an "all bins" or a filtered export, and writes a
CSV. If the list is empty, nothing is written and a message is printed.
}
