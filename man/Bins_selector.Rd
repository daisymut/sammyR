% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_solubility.R
\name{Bins_selector}
\alias{Bins_selector}
\title{Select and Classify Differentially Soluble Bins}
\usage{
Bins_selector(
  combination,
  allmixeddf_grobj,
  fraction1 = "S2S",
  fraction2 = "S3",
  ths = 0.1
)
}
\arguments{
\item{combination}{Character scalar naming the comparison; used as the column
selector into the global \code{pr} matrix to retrieve the two group labels.}

\item{allmixeddf_grobj}{A GRanges of bins with per-sample score columns in
\code{mcols} (typically quantile-normalised).}

\item{fraction1}{Character label of the first fraction (default "S2S").}

\item{fraction2}{Character label of the second fraction (default "S3").}

\item{ths}{Numeric absolute-mean threshold for retaining a bin (default 0.1).}
}
\value{
A named list keyed by group labels, containing:
\itemize{
\item \code{<test>_vs_<ref>_all_shifting_bins} - all classified shifting bins;
\item four per-fraction category GRanges (\code{<frac>_up} / \code{<frac>_down});
\item \code{<test>_genes_<ref>} - gene counts per category;
\item \code{genes} - vectors of overlapping gene IDs per category;
\item \code{<test>_gr_<ref>} - all threshold-passing bins;
\item \code{<test>_allgr_<ref>} - all bins with Cohen's d.
}
}
\description{
Core engine of the differential solubility analysis. For a single group
comparison, computes per-bin confidence intervals for each group, the delta
between them, directional shift calls and Cohen's d, then classifies bins
into up/down categories for two fractions and, when annotation is available,
maps them onto overlapping gene promoters.
}
\note{
Currently relies on \code{pr} (group matrix) and, for gene mapping,
\code{final_genes} being present in \code{.GlobalEnv}. This global-state dependency is
slated for refactor into explicit arguments.
}
\seealso{
\code{\link{import_and_rebin__bw}}
}
